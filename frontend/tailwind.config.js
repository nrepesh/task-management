module.exports = {
    content: ['./src/**/*.{js,jsx,ts,tsx}'], // Scan these files for Tailwind classes
    theme: {
        extend: {}, // Add custom styles here if needed
    },
    plugins: [], // Add Tailwind plugins here if needed
};
